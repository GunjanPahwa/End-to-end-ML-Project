#i will try to  call all component files from here
#A pipeline in ML is a tool (conceptually or programmatically) that chains together multiple data processing and modeling steps so that the entire ML process can be executed as a single unit.
